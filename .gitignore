# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# uv / venv  (uv.lock IS tracked — reproducibility)
.venv/

# data & artifacts  (results/ IS tracked — small JSON/PNG artifacts back the docs+figures;
# the *.pt/*.ckpt rules below still exclude any large model dumps that land there)
data/
*.pt
*.ckpt
wandb/
runs/

# build
build/
dist/
*.so
*.pyd

# editors / OS / Claude session data
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

# LaTeX build intermediates (thesis/ source is tracked; PDF is regenerable via tectonic)
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
thesis/main.pdf

# thesis LaTeX source kept local; the repo ships only the compiled final PDF
thesis/chapters/
thesis/frontmatter/
thesis/main.tex
